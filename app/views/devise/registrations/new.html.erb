<div class="min-h-screen flex items-center justify-center bg-gray-900 text-gray-100">
  <div class="w-full max-w-md p-8 bg-gray-800 rounded shadow-lg">
    <h2 class="text-2xl font-bold text-green-400 mb-6 text-center">Rejestracja</h2>

    <%= form_with(model: resource, url: registration_path(resource_name), local: true, class: "space-y-4") do |f| %>
      <div>
        <%= f.label :email, class: "block mb-1 text-green-300 font-semibold" %>
        <%= f.email_field :email, autofocus: true, class: "w-full p-2 rounded bg-gray-700 text-green-200 focus:outline-none focus:ring-2 focus:ring-green-400" %>
      </div>

      <div>
        <%= f.label :password, class: "block mb-1 text-green-300 font-semibold" %>
        <%= f.password_field :password, autocomplete: "new-password", class: "w-full p-2 rounded bg-gray-700 text-green-200 focus:outline-none focus:ring-2 focus:ring-green-400" %>
      </div>

      <div>
        <%= f.label :password_confirmation, class: "block mb-1 text-green-300 font-semibold" %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full p-2 rounded bg-gray-700 text-green-200 focus:outline-none focus:ring-2 focus:ring-green-400" %>
      </div>

      <div>
        <%= f.submit "Zarejestruj się", class: "w-full py-2 px-4 bg-green-400 text-gray-900 font-bold rounded hover:bg-green-300 transition-colors" %>
      </div>
    <% end %>

    <p class="mt-4 text-center text-gray-400">
      Masz już konto? <%= link_to "Zaloguj się", new_session_path(resource_name), class: "text-green-400 hover:text-green-300" %>
    </p>
  </div>
</div>
