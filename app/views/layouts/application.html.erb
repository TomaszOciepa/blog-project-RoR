<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog Project" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 text-gray-100 font-mono min-h-screen flex">

    <!-- Lewa kolumna: nagłówek + menu -->
    <aside class="w-64 bg-gray-800 border-r border-gray-700 p-6 flex-shrink-0 h-screen flex flex-col">

      <!-- Nagłówek bloga -->
   <h1 class="text-2xl font-bold mb-6 text-green-400 font-mono">
  <%= link_to "📂 Tech Blog", posts_path, class: "text-green-400 hover:text-green-300" %>
</h1>


    <hr class="border-t border-green-400 mb-6">

<% if user_signed_in? %>
  <h2 class="text-xl font-bold text-green-400 mb-6">Menu</h2>
  <ul class="space-y-3 flex-1">
    <li>
      <%= link_to posts_path, class: "block px-3 py-2 rounded hover:bg-gray-700 hover:text-green-400 #{'text-green-400 font-semibold' if current_page?(posts_path)}" do %>
        <span class="mr-1">[Główna]</span>
        <% if current_page?(posts_path) %>
          <span class="animate-blink">█</span>
        <% end %>
      <% end %>
    </li>
    <li>
      <%= link_to my_posts_path, class: "block px-3 py-2 rounded hover:bg-gray-700 hover:text-green-400 #{'text-green-400 font-semibold' if current_page?(my_posts_path)}" do %>
        <span class="mr-1">[Moje posty]</span>
        <% if current_page?(my_posts_path) %>
          <span class="animate-blink">█</span>
        <% end %>
      <% end %>
    </li>
    <% if user_signed_in? && (current_user.writer? || current_user.admin?) %>
    <li>
      <%= link_to new_post_path, class: "block px-3 py-2 rounded hover:bg-gray-700 hover:text-green-400 #{'text-green-400 font-semibold' if current_page?(new_post_path)}" do %>
        <span class="mr-1">[Dodaj post]</span>
        <% if current_page?(new_post_path) %>
          <span class="animate-blink">█</span>
        <% end %>
      <% end %>
    </li>
    <% end %>
  </ul>
<% end %>



  <hr class="border-t border-green-400 my-6">

<div class="mt-auto">
  <% if user_signed_in? %>
    <p class="text-sm text-gray-400 mb-2">
      Zalogowany jako: <span class="text-green-400"><%= current_user.email %></span>
      (<%= current_user.role.capitalize %>)
    </p>
    <%= link_to "🚪 Wyloguj", destroy_user_session_path,
          data: { turbo_method: :delete },
          class: "block px-3 py-2 rounded hover:bg-gray-700 hover:text-red-400 text-red-500 font-semibold" %>
  <% else %>
    <%= link_to "🔑 Zaloguj", new_user_session_path,
          class: "block px-3 py-2 rounded hover:bg-gray-700 hover:text-green-400" %>
    <%= link_to "📝 Zarejestruj się", new_user_registration_path,
          class: "block px-3 py-2 rounded hover:bg-gray-700 hover:text-green-400" %>
  <% end %>
</div>


    </aside>

    <!-- Główna zawartość po prawej -->
    <main class="flex-1 p-8">

      <!-- Powiadomienia -->
      <% if notice.present? %>
        <p class="mb-4 text-green-400 font-semibold"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
        <p class="mb-4 text-red-400 font-semibold"><%= alert %></p>
      <% end %>

      <!-- Zawartość widoków -->
      <%= yield %>
    </main>

  </body>
  
</html>
